# dependencies (setup-peers.mjs creates symlinks here)
node_modules/

# runtime team/protocol state (lives in the *user's* workspace, guard in dev checkouts)
.pair-programming/

# pack artifacts & logs
*.tgz
*.log
.DS_Store

# verification probes (browser cookie jar, fetched web bundles)
cj.txt
combo.js
chunk.js
idx.html
